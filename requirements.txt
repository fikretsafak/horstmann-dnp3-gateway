# Horstmann Smart Logger DNP3 Gateway
# Runtime bagimliliklari

pydantic>=2.5,<3.0
pydantic-settings>=2.1,<3.0
requests>=2.31,<3.0

# NATS JetStream — PRIMARY telemetri yayinlama yolu. Gateway 0.4.x ile
# RabbitMQ telemetri akisindan kaldirildi; tum telemetri JetStream'e
# basilir. Backend tarafindaki alarm/notification akisi RabbitMQ'da
# kalmaya devam ediyor (gateway onunla ilgilenmez).
nats-py>=2.6,<3

# LEGACY — pika sadece rollback senaryosunda (rabbit_publisher.py modulunu
# explicit kullanan kod icin) gerekli. Production gateway pika'ya BAGLI
# DEGIL; bu satir geriye uyumluluk icin tutuluyor. Cutover'a guven gelince
# tamamen kaldirilabilir.
pika>=1.3,<2.0

# Onerilen DNP3 master: yadnp3 (OpenDNP3 reference, Group 110 / Octet String).
# pypi'de yok; wheel asagidaki dizinden kurulur (install.ps1).
# yadnp3>=3.2,<4.0  # _wheels/yadnp3-3.2.1.1-...whl

# Legacy (Group 110 yok, OpenDNP3 outstation ile tutarsiz):
nfm-dnp3>=1.0.1,<2.0
